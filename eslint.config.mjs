// Mínimo para Next 16 + TS. Lint roda via `next lint` que carrega esta config.
// Para regras adicionais, ver https://nextjs.org/docs/app/api-reference/config/eslint
export default [
  {
    ignores: [".next/**", "node_modules/**", "out/**"],
  },
];
